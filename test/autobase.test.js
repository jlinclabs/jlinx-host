const b4a = require('b4a')
const {
  keyToBuffer,
  sign,
  createSigningKeyPair
} = require('jlinx-util')
const request = require('supertest')

const createHttpServer = require('../http-server')
const { test } = require('./helpers/index.js')

test('autobase', async (t, createHost) => {

})
